language: ruby
sudo: false
cache: bundler
rvm:
  - 2.1.1
bundler_args: --with test
before_script:
  - npm install surge
script:
  - make test
after_success:
  - make stamp
  - test $TRAVIS_BRANCH == "surge" && surge --project ./public --domain https://dev.socrata.com
